/**
 * Application configuration constants
 */

export const APP_CONFIG = {
  DEFAULT_BUDGET: 150,
  DEFAULT_PLAN: "Free" as const,
  LOADING_DELAY_MS: 1500, // Simulated loading delay for demo purposes
} as const;


import { Wallet, Home, Zap, Target } from "lucide-react";
import type { LucideIcon } from "lucide-react";

/**
 * Goal icons mapping for onboarding step 1
 */
export const GOAL_ICONS: LucideIcon[] = [Wallet, Home, Zap, Target];

